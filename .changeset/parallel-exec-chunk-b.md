---
type: Added
pr: 21
---
<!-- docs-exempt: Chunk B is internal engine wiring; user-facing docs land in Chunk C (ADR 0014). -->
**Parallel multi-phase wave executor — failure isolation + per-phase base SHA + sole-writer tripwire (Chunk B of ADR 0014, issue #13 gap 2).** Extends `GSD.runParallel`'s wave loop (`parallel-runner.ts`) with the Chunk B decisions: **D3** — each wave re-checks-out the protected branch at its boundary (`onWaveStart`) so every phase forks off the protected HEAD as the prior wave's promotes left it, capturing its OWN base SHA rather than a stale global LAST_GOOD; **D2** — a wave-scoped sole-writer tripwire (`assertLedgersClean`) that fails closed if a phase-agent left conflict markers in `ROADMAP.md`/`STATE.md` on protected after a wave; **D4** — continue-independents / skip-dependents on failure: a failed (or itself-skipped) phase's transitive `depends_on` dependents are skipped (populating `PhaseParallelOutcome.skippedReason`) while unrelated phases proceed, with each dependent awaiting its in-run predecessors' settlement before deciding (conflict-graph schedules by files only, so a dependent can share a wave with its predecessor); the failed phase's never-promoted integration branch is discarded by the EXISTING Tier-1 rollback, reused unchanged at wave boundaries. Chunk B of 3; C (nested global budget / config / `/gsd-execute-parallel` command / docs) follows.
